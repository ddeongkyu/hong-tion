# Supabase

Run the SQL files in `sql/` from lowest number to highest number.

These files are written so they can be pasted into the Supabase SQL Editor.
Later, we can convert the same schema into Ecto migrations once the Phoenix
database connection is configured.
